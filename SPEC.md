# 故事塔罗牌 · 产品规格

> 本文件是唯一的需求来源。实现前先读完本文件。

---

## 0. 一句话

把《物語の体操》（大塚英志，星海社新書 35）第一讲的「24 张卢恩卡片抽 6 张摆位置」做成一个
**可维护、可扩展、功能拉满的本地 Web 应用**，并对外提供 **JSON API 获取抽卡结果**。

---

## 1. 原始规则（来自原书第一講・图 3／图 4／图 6，不許改动其含义）

### 1.1 牌组：24 个卢恩词（原始牌组，原文照录）

| 行 | 词（日文原词） | 中文 |
|---|---|---|
| 1 | 意思 / 清楚 / 理性 / 秩序 / 知恵 | 意志 / 清秀 / 理性 / 秩序 / 智慧 |
| 2 | 誓約 / 善良 / 節度 / 至誠 / 生命 | 誓约 / 善良 / 分寸 / 至诚 / 生命 |
| 3 | 寛容 / 解放 / 調和 / 創造 / 信頼 | 宽容 / 解放 / 调和 / 创造 / 信赖 |
| 4 | 公式 / 変化 / 結合 / 厳格 / 勇気 | 公式 / 变化 / 结合 / 严格 / 勇气 |
| 5 | 幸運 / 庇護 / 治癒 / 慈愛 | 幸运 / 庇护 / 治愈 / 慈爱 |

共 24 张，每个词在原文中另配一个卢恩符文符号（符号仅作装饰，**无实质含义**）。

### 1.2 六个位置（原文含义，顺序固定）

| 位号 | 日文 | 中文 | 布局（相对位置） |
|---|---|---|---|
| 1 | 主人公の現在 | 主角的现在 | 中心 |
| 2 | 主人公の近い未来 | 主角的近未来 | 中心右侧 |
| 3 | 主人公の過去 | 主角的过去 | 中心左侧 |
| 4 | 援助者 | 援助者 | 上方 |
| 5 | 敵対者 | 敌对者 | 下方 |
| 6 | 結末（目的） | 结局（目的） | 最右 |

时间线：3 → 1 → 2 → 6；4 在上（助力）、5 在下（阻力）。

### 1.3 抽卡规则（原文）

1. 洗牌；
2. 逐张抽出 **6 张**，字面朝上；
3. 按抽到的顺序摆到 **图 4 的编号位置**；
4. **抽到倒置的牌，保持倒置**（逆位置，含义反转）；
5. **可以无限次重抽，但不许看着牌面为了顺手而重新摆位置——那是犯规。位置交给运气。**

### 1.4 原书明确授权「改编概念」（这是产品需求，不是附加功能）

- 「卡片上写的词没有特别依据」——`生命`/`秩序` 可以换成 `努力`/`友情`/`胜利`；
- 可以翻字典随便挑二三十个词，自制一整副牌；
- 也可以直接用塔罗牌（占星家镜リュウジ也说过塔罗能做故事训练）。

牌面**只有一个词**（原书如此）：不写释义，不写逆位释义，不写引导问句——写上解释，使用者的联想就被
替做完了，「联想人物和事件」这一步也就消失了。

因此：**牌组、位置方案、正逆标签、文案，全部必须数据驱动 + 可在界面里改编 + 可导入导出。**

---

## 2. 硬性需求

### R1 可维护
- 单一技术栈、TypeScript、模块边界清晰、无魔法字符串；
- 所有领域数据（牌组 / 位置方案 / 正逆标签 / 文案）外置为 JSON，代码不硬编码任何一张牌；
- README 说明：**新增一整套牌组 = 新增一个 JSON 文件 + 注册，零改代码**。

### R2 可扩展
- 牌组（deck）、位置方案（layout）、解读策略（interpretation）、主题（theme）四类插件点，各自独立；
- 抽卡引擎不依赖 UI，可被 API 单独调用；
- 后续可加：自定义洗牌算法、加权/禁用某张牌、多套位置方案（如三幕 8 位、英雄之旅 12 位）。

### R3 功能拉满（默认要实现，不要砍）
- 牌桌可视化：按 1.2 的相对位置渲染，逐张翻牌动画，逆位明显可辨；
- 抽卡：随机 or 指定 seed（**seed 必须可复现同一结果**）、逆位概率可配（0 / 25% / 50%）；
- 解读面板：逐格列出「位置 + 词 + 正逆」——牌面只有词，面板不提供释义、问句或成句提示；
- 历史记录：本地保存每次抽卡（含 seed、时间、牌面），可回看、可删除；
- 分享链接：URL 携带 deck/layout/seed/reversed，打开即复现；
- 导出：JSON / Markdown（可直接贴进小说笔记）/ 结果卡片图片；
- 概念编辑器：网页内增删改词组与位置方案，导出/导入 JSON，恢复默认；
- 内置多套牌组：至少「原版卢恩 24」+ 一套演示「努力/友情/胜利」+ 一套塔罗大阿尔卡纳 22（塔罗可选做）；
  本项目另有 `hanzi24`（24 个汉字，一字一牌）；
- 中日双语切换（`日文原词 / 中文` 对照显示）。

### R4 JSON API（明确要求）
- `GET /api/health`
- `GET /api/decks` —— 列出所有牌组与位置方案（含词的完整数据）
- `GET /api/draw?deck=<id>&layout=<id>&seed=<string>&reversed=<0|25|50>` —— 返回一次抽卡结果
- `POST /api/draw` —— 请求体直接携带自定义牌组/位置，用于外部脚本一次调用
- 其它：`GET /api/history`、`POST /api/decks`（保存自定义牌组）
- 返回结构示例（字段名可调整，但必须包含这些语义）：
```json
{
  "deckId": "runes24",
  "layoutId": "odaiba6",
  "seed": "story-20260915",
  "drawnAt": "2026-09-15T12:00:00Z",
  "cards": [
    { "slot": 3, "slotLabel": "主人公の過去", "slotLabelZh": "主角的过去",
      "word": "至誠", "wordZh": "至诚", "reversed": false, "rune": "ᛉ" }
  ]
}
```
- 返回体只含事实：位置（编号 / 名称 / 坐标）、词（与装饰符号）、正逆；**不含**释义、问句或成句提示。
- API 必须与 UI 使用**同一个抽卡引擎**（同一 seed 得到同一结果）。

---

## 3. 环境与交付

- 运行环境：Windows 11，Node **v24.16.0**，npm **11.13.0**，bun **1.4.0**，git 2.55。shell 为 pwsh。
- 工作目录：`C:\Users\Moeblack\Repos\story-tarot`（本目录即仓库根）。
- 启动方式：**一条命令**（如 `npm run dev`），无需外部服务、无需数据库、无需联网。
- 交付物：可运行代码 + README（含「怎么加一套新牌组」「怎么调 API」两节）+ 本规格文件的实现对照说明。

## 4. 边界

- 不做用户系统、不做云端、不做付费；
- 不引入需要外部 API Key 的依赖；
- 不写测试套件、不做额外验证流程（用户未授权测试与验收）。
- 必须实际编译/构建通过（这是产出物本身，不属于测试）。
